# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Environment / secrets — never commit
.env
.env.*
!.env*.example
**/.env.local
**/.env*.local

# Shopify CLI
.shopify/
dist/

# OS / editor
.DS_Store
*.log

# Local agent config
.claude/

# Vercel CLI project link (ids, not secrets — still not repo material)
.vercel/

# Superseded material, kept locally only. Safe to delete once you are sure.
/_junk/

# Brand source assets — large originals. Processed versions ship in
# spritz-site/public/. See brand/README.md.
/brand/guideline.pdf
/brand/labels/
/brand/vandal-ds/
/brand/bottle-photos.zip
*.zip

# Generated video output + LUT previews (rebuild: video/luts/generate-lut.py)
/video/out/
/video/luts/preview/

# TypeScript incremental build cache
*.tsbuildinfo
